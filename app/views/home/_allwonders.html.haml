.page-header
  %h1 Чудеса света
- Chudesa.all.order('sortorder ASC').each do |chudesa|
  .showplace_block.nonaklonniytext
    %p
      %b=chudesa.name
    %span=chudesa.toptext
    .shpage_null
      -#.prevsh
        =image_tag '/img/content_arr_slider_left.png'
      .contsh
        - chudesa.showplaces.each do |showplace|
          = render :partial => 'shared/showplace', :locals => {:showplace => showplace}
      -#.nextsh
        =image_tag '/img/content_arr_slider_right.png'
    %span=chudesa.bottomtext
  %br
  %br
